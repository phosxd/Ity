- Add ability to import other scripts as modules.

- Replace variable name string lookup in scope data, with unsigned integer ID lookup by converting every declared name to an ID during tokenization, only reverting back to a string for errors.
  This is the single largest performance improvement I could make right now & through CPU profiling it was revealed that hash lookup in scope is (directly) responsible for 21.8% of CPU time.
 

Add more FileAccess methods:
	- read_file_bytes
	- write_file_text
	- write_file_bytes
	- file_exists
	- dir_exists
	- get_items_in_dir


Add opt-in libraries:
	- JSON
	- HTTP
	- DBus (linux only)
	- raylib
	- raygui
