
- Add simple array data type (std::vector).
- Add simple dictionary data type (std::unordered_map).

- Fix negative number Variants' type being "NONE" when being used as an operand (????). Probably a tokenization error.

- Add ability to nest practically unlimited composite instructions.
- Add "while" instruction to make it easier to create a conditional loop.
