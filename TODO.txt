- Add global property that specifies whether or not a script is running as a module.
- QOL feature: allow some operators to capture all RHS tokens via recursive decent.
- QOL feature: separator symbol `,` should automatically wrap tokens into a grouping. Allows direct operator usage in arrays & maps.

- Fully implement StrUtil & ArrUtil modules.


Add opt-in C++ modules:
	- JSON
	- HTTP
	- DBus (linux only)
	- raylib
	- raygui
