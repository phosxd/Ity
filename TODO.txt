- Add `STR` type methods: `split`, `join`, `erase`.
- Add built-in `range` function which generates an array of numbers with the given start, end, & step.
- Add ability to import other scripts as modules.


Add standard built-in libraries:
	- FileAccess


Add opt-in libraries:
	- JSON
	- HTTP
	- DBus (linux only)
	- raylib
	- raygui
