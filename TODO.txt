- Add ability to import other scripts as modules.
- Store functions in a dedicated `FUNC` type instead of a `MAP`, avoids multiple hash table lookups per individual native or script function call & saves on memory.
 

Add more FileAccess methods:
	- read_file_bytes
	- write_file_text
	- write_file_bytes
	- file_exists
	- dir_exists
	- get_items_in_dir


Add opt-in libraries:
	- JSON
	- HTTP
	- DBus (linux only)
	- raylib
	- raygui
