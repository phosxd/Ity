merge IO; merge Time; merge FileAccess;

const INT start = now:'ms';
const STR blob = read_file_text:'Benchmark/Allocation/Blob.txt' * 100_000;

print:(size:blob);
print:['\nDone in ', (now:'ms'-start / 1000.0), 's.'];
