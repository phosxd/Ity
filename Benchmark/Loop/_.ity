merge IO; merge Time;

const * n = prompt:['Count: '] -> INT;
const INT start = now:['ms'];

var INT i = 0; while i < n; set i += 1;
	print:[i];
/;

print:['\nDone in ', ((now:['ms']-start) / 1000.0), 's.'];
